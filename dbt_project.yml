name: "aegis_data_reliability"
version: "1.0.0"
config-version: 2

profile: "aegis_data_reliability"

model-paths:
  - "dbt/models"

test-paths:
  - "dbt/tests"

seed-paths:
  - "dbt/seeds"

macro-paths:
  - "dbt/macros"

snapshot-paths:
  - "dbt/snapshots"

clean-targets:
  - "dbt/target"
  - "dbt/dbt_packages"

models:
  aegis_data_reliability:
    staging:
      +materialized: view

    silver:
      +materialized: table

    gold:
      +materialized: table
